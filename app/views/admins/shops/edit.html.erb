<% breadcrumb :edit_admins_shops %>
<%= render "admins/shared/flashes" %>
<div class="card col-sm-9 mx-auto mt-5 mobile-card-border">
  <div class="card-body">
    <h3 class="text-center mt-3 mb-5 fw-bold">店舗情報編集</h3>
    <%= form_with model: @shop, url: admins_shop_path, local: true  do |f| %>
      <%= render partial: 'form', locals: { f: f } %>
    <% end %>
  </div>
</div>
